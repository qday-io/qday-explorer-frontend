# QDAY2 chain identity — baked at build time (chain-specific, rarely changes)
# URL/host configuration — injected at runtime via docker-compose environment vars
# See: qday2-deploy/env.blockscout.example for runtime vars

NEXT_PUBLIC_APP_ENV=production

# Network
NEXT_PUBLIC_NETWORK_NAME=QDay Testnet
NEXT_PUBLIC_NETWORK_SHORT_NAME=QDay
NEXT_PUBLIC_NETWORK_ID=44005
NEXT_PUBLIC_NETWORK_CURRENCY_NAME=QDAY
NEXT_PUBLIC_NETWORK_CURRENCY_SYMBOL=QDAY
NEXT_PUBLIC_NETWORK_CURRENCY_DECIMALS=18
NEXT_PUBLIC_NETWORK_CURRENCY_WEI_NAME=bit
NEXT_PUBLIC_NETWORK_CURRENCY_GWEI_NAME=Qbit
NEXT_PUBLIC_NETWORK_VERIFICATION_TYPE=validation

# Stats — baked in to enable the feature flag at SSR time (module-level const).
# Runtime value (window.__envs) is set via docker-compose NEXT_PUBLIC_STATS_API_HOST.
NEXT_PUBLIC_STATS_API_HOST=https://explorer-test.qday.io

# UI
NEXT_PUBLIC_HOMEPAGE_CHARTS=['daily_txs']
NEXT_PUBLIC_AD_BANNER_PROVIDER=none
NEXT_PUBLIC_NAVIGATION_LAYOUT=horizontal
NEXT_PUBLIC_COLOR_THEME_DEFAULT=dark

# Branding
NEXT_PUBLIC_NETWORK_LOGO=file://./public/static/logos/qday-logo-horizontal-blue.png
NEXT_PUBLIC_NETWORK_ICON=file://./public/static/logos/qday-logo-horizontal-blue.png
NEXT_PUBLIC_OG_DESCRIPTION=QDay Testnet Explorer — explore blocks, transactions, addresses and tokens on the QDay test network.
NEXT_PUBLIC_PROMOTE_BLOCKSCOUT_IN_TITLE=false

# API spec
NEXT_PUBLIC_API_SPEC_URL=https://raw.githubusercontent.com/blockscout/blockscout-api-v2-swagger/main/swagger.yaml

# Testnet flag
NEXT_PUBLIC_IS_TESTNET=true
